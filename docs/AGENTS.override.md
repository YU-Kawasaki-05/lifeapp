# docs/AGENTS.override.md

## Scope
- This override applies to all files under `docs/`.

## Expectations
- Keep requirement IDs (`FR-*`, `SCR-*`, `BR-*`, `U-*`) unchanged unless explicitly requested.
- Prefer incremental edits over full rewrites to preserve reviewability.
- If a decision changed, update `docs/00_共通/決定事項ログ_decision-log.md` in the same change set.
